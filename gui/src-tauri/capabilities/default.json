{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The main window may call the app's own commands and receive its events. No plugins, no network, no filesystem API: all device access goes through the Rust commands.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
