[package]
name = "xtask"
description = "Workspace automation: the no-network dependency audit and the CI gate."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false

[dependencies]
serde_json.workspace = true
